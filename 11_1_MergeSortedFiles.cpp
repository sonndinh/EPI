#include <iostream>
#include <algorithm

using namespace std;

void merge(vector<int>& infs, vector<int>& of) {
	
}

int main() {
	// Simulate 500 files, each contains a list of increasing trade times.
	vector<int> infiles[500];
	vector<int> outfile;
	merge(infiles, outfile);
	
	return 0;
}

#include <iostream>
#include <string>
#include <unordered_set>
using namespace std;

// Return the shortest prefix of a string which is not a prefix of any string in a dictionary.
string shortest_prefix(const string& word, const unordered_set<string>& dict) {
	
}

int main() {
	return 0;
}

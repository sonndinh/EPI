#include <iostream>
#include <vector>
#include <unordered_set>
#include <string>
using namespace std;

// Return the starting and ending indexes of a smallest subarray that covers the keywords.
pair<int, int> smallest_subarray(const vector<string>& A, const unordered_set<string>& keys) {
	
}

int main() {
	return 0;
}
